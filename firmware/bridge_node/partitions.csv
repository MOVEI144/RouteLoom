# RouteLoom gateway (bridge) layout (issue #37,
# docs/design/sdk-v1/05-nvs-state-37.md section 5.2). Per-peer security state
# (rlcounter/rlreplay) lives in its own "rlsec" NVS partition, sized for the
# gateway's larger peer set, so exhausting it can never block the boot
# session (rlboot) or other system NVS writes. Ends at 0x1B0000: fits the
# ESP-IDF default 2 MB flash-size setting and every 4 MB board. Changing this
# table moves NVS: erase flash when migrating (already required by Wire v2).
# Name,   Type, SubType, Offset,   Size
nvs,      data, nvs,     0x9000,   0x6000
phy_init, data, phy,     0xf000,   0x1000
factory,  app,  factory, 0x10000,  0x180000
rlsec,    data, nvs,     0x190000, 0x20000
