# 実装状況とリリース条件

更新：2026-09-26（このworktreeの基点 `d4ef290`。PR #155／#156／#158 を含む）。

zcbor 0.8.1 の同梱ソースには、libedhoc が渡す長さ0の protected header で null ポインタを `memmove` に渡さないための1行修正がある。libedhocにも EDHOC m2／m3／m4 の末尾余剰バイトを拒否する修正がある。upstreamとの差分と固定 blob id は `components/routeloom/third_party/VENDORED.json` と `tests/test_vendored_sources.py` で検査する。以下の「upstreamのまま」という記録は、これらの修正を除く。

## 現在の証拠

| 項目 | 状態 |
|---|---|
| 統合アーキテクチャ・無線・SDK契約 | 文書化済み。Wire v2 byte layout・type IDはCORE_FIXED_250向けに凍結（test cipher vector付き。v1から32bit epoch／48bit counterへ改訂、#29）。本番Security Profileは未凍結 |
| Portable C++ SDK core | **実装済み・host-tested**。GCC/Clang、ASan/UBSan、有限Queue、配送、dedup、receipt、Babel由来routing、C ABI |
| ESP-NOW / LR250 adapter | **実装済み・build-tested**。単一Radio Owner、固定channel、Peer、短いcallback queue、NVS counter/replay。2台C3で実電波配送・END_RECEIPT往復を手動smoke確認。到達距離・干渉・長期・資格は未認定 |
| ESP32 reference firmware | **C3/S3/C5でESP-IDF v6.0.3 build成功**。bridge_nodeはC3実機起動・Rust daemonとの実USB sessionを確認。2台C3（bridge_node＋reference_node）で実ESP-NOW配送・END_RECEIPT往復を手動smoke確認（連続6送＋reset後再送）。HIL suite・マルチホップ・長期は未実施 |
| Rust host service／CLI／TUI | **実装済み・host-tested**。fmt、Clippy `-D warnings`、unit test、release build成功。daemonはUnix socket経由でSTATUS/SENDに加えADAPTER/NODES/DELIVERIES/EVENTS/AUTHORITY/AUTONOMYを返し、routeloom-tuiは同一JSONをpollする観測者（daemon切断時はbackoff再接続、有界event ring）。情報源の無いfieldは`unknown`表示。API1にpush型受信（`messages.subscribe`/`unsubscribe`/`subscriptions`、`messages`+`events`両stream、per-subscription staging・gap/overflow marker・heartbeat・≤15s long-poll・接続死亡で購読消滅、sdk-completion/05設計準拠）を実装・host試験済み。group／ALL配送のAPI1 `group.send`（SEND権限、idempotency key、`wait_ms`で受理待ち）／`group.get`（READ_OPERATION、`wait_ms`でFINAL待ち）と`group_settled` eventを実装：USB 0x50〜0x52を専用laneで駆動し、即時0x51とFINALに加え未決着の間は0x52で読み直す。capability bit 7無しは`UNSUPPORTED`、送られたか不明な送信は再送せず`INDETERMINATE`、RAM表は有界（256件・host待ち8・未決着16・墓標1024）。アプリ向け`routeloom-client`に`send_group`／`group_result`（4操作契約は不変）、`routeloomctl group-send`／`group-get`を追加（host試験のみ、実機未確認）。実C3 adapter経由のsession確立・配送観測・reset後自動再認証を手動smoke確認（HILではない） |
| USB／Serial transport | **Portable実装・host-tested**。COBS＋CRC-32/ISO-HDLC codec、streaming resync、HELLO→AUTH→ACTIVE→DRAINING session、EXPERIMENTALな開発profile認証（共有secret＋transcript結合MAC、方向別counter・replay拒否）、累積credit、MeshNode統合をC++ device bridgeとRust hostで実装。`protocol/usb-golden`共有vectorでbyte相互検証。加えて`firmware/bridge_node`（USB Serial/JTAG配線）をESP32-C3へ書込み、Rust daemonとの実USB serial上でHELLO→AUTH→ACTIVE session確立、sealed DataToMesh受理・delivery event・累積credit授受を確認済み（手動smoke、HIL suiteではない）。デバイスreset後の再接続でdaemonの初回Helloが起動中に失われ停滞する不具合を実機で発見し、writer threadのbounded Hello retry（1s cadence、Active到達まで）で修復。reset→自動再認証→配送完了まで実機確認。加えてdaemon側にセッション損失検知（stale-session echo・fatal error・tag desyncで即時再Hello）と無応答watchdog（3×keepalive無受信でin-place再ハンドシェイク）、adapter openのexponential backoff、`link.get` API（disconnected/reconnecting/attached＋lane登録状態の可視化）を実装・host試験済み。複数機・長期・本番Profileは未認定 |
| 暗号Provider | PSA AES-GCM、context（scope＋network＋sender＋receiver＋epoch）結合HMAC導出、耐電断counter予約（追い出し時はRAM checkpointで区間再開）、永続replay上限（RAM windowの前方に予約するaccepted ceiling、counter 65前進毎に1 commit）＋peer epoch floor（同epochのwindow消失は拒否・新epochで再開、破損recordはIntegrityError、commit失敗は受理巻戻し）を持つ開発PSK Providerを実装。replay規則はportable ReplayGuardとしてhost試験済み（ESP側NVS adapterはbuild check、実機未試験）。ピアごとのcounter/replay recordは専用NVS partition `rlsec`（通常64KiB／bridge 128KiB）に置き、起動時の旧epoch TX record掃除（証人`cmax`を先にcommit）と永続ピア数上限（通常64／gateway 128、超過した新規ピアは`PEER_STATE_CAPACITY`で拒否・計数）で有界化した（#37のP0緩和、portable coreで200ピアchurnをhost試験、NVS予算はCIで計算検査、実機未計測。P4でRAM sessionへ移行し、独立した耐久移行markerと電源断行列を追加。旧版機のrlsec満杯時の復旧経路は#152、[sdk-v1/05 §9](design/sdk-v1/05-nvs-state-37.md)）。本番session engineへの接続口として`SecurityProvider::tx_epoch()`／`context_state()`、`SessionInstaller`、予約scope `GroupLink`（3、既存`Group`＝2は不変）を追加し、送信epochをProviderから取る配線とsession未確立時の保留（`SESSION_REQUIRED`／`SESSION_PENDING`、期限で`SESSION_UNAVAILABLE`、counterは消費しない）をMeshNodeに入れた（P4-1、[sdk-v1/03 §8.1](design/sdk-v1/03-key-hierarchy.md)）。LegacyFixtureの開発PSK Providerは設定epochを返し挙動・golden vectorは不変（V1-K10をhost試験）、Member EDHOC／RLRES1のRAM session Provider、永続membership／RLP2、SecurityCoordinatorをP4第1〜3段で実装・host試験し、OwnerとMeshNodeに配線済み。実機・HILと本番認定は未実施。`SecurityProvider::security_profile()`は開発Profileで`Development`を返し、Node起動診断とfirmware logでEXPERIMENTALと表示。アプリDATA/END_RECEIPTは常にend保護必須で、未保護frameは診断付き拒否（通常経路に平文DATAなし）。LegacyFixtureのPSKは本番Identityではない。Member EDHOC経路もP8認定前 |
| 経路制御 | feasibility、withdraw、SeqNoRequest、generation／tombstone／hold-down、bounded seqno、3hop／diamond repairに加え、10hop配送、分断再結合、loop-freedomをportable model testで実装・確認。100台向けgateway-scoped profile（issue #41、[設計](design/sdk-v1/routing-scale.md)：gateway木の周期更新・上り部分木・ROUTE_REQUESTによるpull／on-demand探索、lease規則をconfig検査で強制）をportable coreに実装し、100台SimWorld（5s tick／90s lease、4分間gateway往復到達の途切れ0・loop 0・推定制御airtime 78ms/s）でhost試験済み。`NodeConfig::route_gateways`を未設定なら従来のflat profile。C API（`rl_node_config_t`末尾の`route_gateway_count`／`route_gateways`、`RL_ABI_VERSION`は2のまま）とfirmware Kconfig（`ROUTELOOM_ROUTE_GATEWAY_SCOPED`、bridge_nodeは自身をgatewayに載せる、scoped時5s／90s、lease規則違反はbuild失敗）へ配線し、C APIはhost試験済み・firmwareはCIのroute_scoped cellでbuildのみ確認。hostへのprofile報告（USB HostOps）と実RFでの認定は未実施 |
| group／ALL配送・順序 | KGuard向けのgroup配送（[設計](design/sdk-v1/group-delivery.md)）をportable coreに実装・host試験済み：gateway-scoped木に沿った子ごとのMAC ACK付きunicast（新frame type 25 GROUP_DATA／26 GROUP_REPORT、Wire v2 headerは不変、flat profileは明示的に非対応）、辺ごとの集約report、欠けた部分木だけへのrepair round（最大12）、送信元のairtime token bucket（既定300ms/s、Urgentは借越し）、membership（ALL＋最大8 group、非memberも中継）、(送信元, stream番号)の重複排除、ORDEREDの有界保持（最大10s、欠番skip＋late表示）とunicast RELIABLEの送信側ordered、`SecurityScope::Group`（開発PSKとtest securityに実装、RAMのみの有界replay表）。100台SimWorldでALARMは全99台へ45ms・summary完了90ms・推定air time 1.46s、MAC損失10％／無音損失5％でも全台受信と正確なsummary、20件burst中もroute control 82ms/s・上り遅延はgroup無しと同一。C ABI（`rl_send_group`等、`RL_ABI_VERSION`は2のまま）とUSB HostOps 0x50〜0x52（capability bit 7、`protocol/usb-golden/group-ops`）もhost試験済み。bridge_nodeへの配線（Kconfig capability bit 7）はCIのfirmware build対象でlocal未build。daemon API1（`group.send`／`group.get`、`group_settled` event、[host §10](spec/host.md)）とrouteloom-clientの`send_group`／`group_result`、`routeloomctl group-send`／`group-get`もhost試験済み。group状態はRAMのみでsleep imageへ永続化しない：drainは収集中のround完了を待ち、commit成功後に未決着originを明示終端（Fail=`SLEEP_DRAIN`、Save=`SLEEP_GROUP_NOT_PERSISTED`、Defer=`SLEEP_DEFERRED`）し、受信側のORDERED holdを欠番skip経路でアプリへ放出する（#110、host試験済み）。callback中のcoordinator操作はBusyで拒否し（呼び直しはアプリ責任）、本文1件の強制hold放出・確定済みだけのcarry・入眠直前の再検証を行う。r1〜r3回帰とcallback×操作試験をhost試験範囲に含める。membershipの遠隔設定、membershipによる部分木の刈り込み、実RF・実機での認定は未実施 |
| 管理 | SingleAuthorityの2スロット耐電断台帳（magic/length/schema/seal、hash chain、CRC-32/ISO-HDLC、readback検証、全損時QUARANTINED＋明示recover）をportable実装・host power-cut試験済み。NVS LedgerStorage adapterはbuild-tested。remote config本体（challenge/permit/journal・Rust発行・`routeloomctl config-*`）に加え、quarantine／uncertainからの復旧経路（署名済みRCR2 recovery object・独立RLF1床・provider readback・ControlObject kind4・HostOps 0x24／RecoveryInfo 0x27）とroot署名RTM1のunicast更新（kind5・HostOps 0x25〜0x26・trust.install/status）をportable実装・host試験済み（issue #51：Rust dev HMAC／COSE両profile発行とTrustView配備でのauthority災害後の世代移行T06・ledger停止中のroot配送を含み、Rust署名→C++検証の共通goldenで相互運用を確認。初回床/root配備・dev/static profileの世代移行・SAK交換・全媒体rollback・防災inventory自動化は対象外、旧firmwareへの適用はmaintenance migrationが必要、実NVS電断・RF/HILは未実施）。quorum／自動選挙／snapshotは未実装 |
| 電源管理 | Portable PowerCoordinator（RUNNING→DRAINING→PERSISTING→READY_TO_SLEEP→SLEEPING→RESUMINGの明示state machine、SleepTicket無効化、2スロットCRC電源image、durable pending復元、TIME_UNCERTAIN規則、bounded resume＋discovery fallback）を実装・host model試験済み。ESP-NOW PowerPort／NVS image／reference firmwareのdeep-sleep経路は`ROUTELOOM_DEEP_SLEEP`選択時のみ配線・build-tested。Group laneはRAMのみでsleep image非対象：収集中roundをdrain完了条件に含め、commit成功後に未決着originをpolicy別に明示終端しORDERED holdを順序通り放出する（#110）。callback中のcoordinator操作をBusyで拒否し、確定済みだけcarry、入眠直前にticket再検証する。r1〜r3回帰とcallback×操作試験をhost試験範囲に含める。P4 Ownerのsleep保存・復元、親binding再確認、全work drain、送信counterのRTC先行更新と経過時間上限は#149でhost試験済み。実機のRTCドリフト・起動遅延（#148）、G-POWERの実機drain／wake／NVS／電池側energyとHILは未実施 |
| Board/RF | 公式資料を整理。2台C3で実ESP-NOW配送（約57ms・連続6送成功）を手動smoke確認。S3/C5現物照合、HIL、到達距離、都市部干渉、マルチホップ、電池寿命は未実施 |
| 高度機能 | LR500適応、Mesh OTA、LoRa TX、service failoverは未実装。自律mesh（issue #3〜#5：RLD1発見・輻輳backpressure・負荷対応経路・channel移行基盤）はEXPERIMENTALなportable実装＋ESP-NOW配線がこのbranch上にあり、host test／firmware build check済み。輻輳→経路結合の安全装置（refusal-pressure floor・非対称slew・dirty-window provenance gate・peer identity reset・sojourn EWMA window decay・ROUTE_REFRESH_BOUND_EXCEEDED診断）を実装し`sdk-completion/03-congestion-routing.md`のチェックリストをhost test済み。加えて`EspNowScopeProvider`（Kconfig選択の開発scope key、ScopeMode指定・世代ring・opaque ScopeRef）を両firmware mainへ配線し、default/scoped両構成でbuild確認済み。既定OFF・migrationはObserve初期モードで、AutoGuardedは説明可能なprecondition gateを全通過した要求のみ明示opt-in。実機・RF・HIL・本番security認定は未実施 |
| 配送保証 | `DeliveryClass::Applied`（design sdk-completion/01）を実装：END_RECEIPTはSDK受理証跡のみで、アプリ判定はend保護経路のAppResult=19フレーム（RESULT/QUERY/RESULT_ACK/STATUS、request digest＋lease＋message identity＋destination結合）で返る。RESULT未到達はDelivered/FailedではなくIndeterminateへ解決。dedup保持（design 02）はLive/Resolved/Evidence/Terminalの位相別eviction順位＋terminal reserve＋per-upstream cap＋飽和counter（DedupStats）でexactly-onceを弱めず圧力を正直にBUSY化。保持は役割別（#39）：中継の転送記録はframe自身の残期限＋5秒、終端pinはorigin期限＋late result30秒（初回受理から最大60秒）。容量はbuild時のresource profile定数（leaf32／relay96／gateway256、既定relay）。3台line・2源×1.5msg/s×120秒で拒否・eviction 0、60秒超sleepの永続pendingはEXPIREDで再送しないことをhost試験。いずれもhost model試験済み、実機未試験 |
| 認証・credential | 本番provisioning lifecycle（design sdk-completion/04）のportable codecを実装・host試験済み：RLT1 dual-slot trust store（anchor・authority key・失効record・epoch/generation floor・quarantine＋明示recover）、RLC1 device credential、RTM1 COSE署名manifest、TrustView（store-backed ConfigPermitVerifier：Active鍵解決＋generation floor＋失効照合、quarantine時fail-closed）。Host側`routeloom-provision`クレート（RLT1/RLC1 image生成・RTM1署名・offline verify・NVS blob生成・RootSigner seam）と`routeloomctl provision-*` verbsを実装・golden vector byte一致試験済み。ESP-NOW側はNVS trust/credential adapter（dual-slot NVS persistence）とKconfig opt-inのproduction TrustView verifier配線（旧provisioning Profileの開発PSK経路とは分離し、有効時はdev envelopeを受けないfail-closed。SDK v1 firmwareの公開既定はDevRam）までbuild-tested。旧RLT1/RLC1向けmaintenance console・パーティション変更・製造flow・実機鍵配備は未実施。開発PSK/HMACは別経路として存続。SDK v1（G-SEC）の部品としてRLCW1証明書（DevCert／SiteCert／MemberCert、CWT＋ES256 COSE_Sign1、canonical CBORのみ・low-S）とRLI1／RLS1／RRS1／RLP1記録codec、2スロット耐電断store（全write・全byte境界の電源断注入試験）をportable coreとRust（`routeloom-provision`の`sdkv1`）に実装し、独立Python生成器の共通vector（`protocol/sdkv1-golden`、RFC 6979署名をRust側で再現）でbyte一致をhost試験済み（[sdk-v1/08 P1-2／P1-3](design/sdk-v1/08-implementation-plan.md)）。ゼロタッチ参加のEDHOC EAD（JoinIntent／SiteOffer／JoinRequest／JoinResult／SitePackage）とRemovalNoticeのcodec・§10.2のAllow検査をportable coreとRust（`routeloom-join`、Site Authority側）に実装し、独立Python生成器の共通vector（`protocol/sdkv1-golden/ead`、MemberCert・RemovalNoticeをRust側でRFC 6979再発行）とfuzzでhost試験済み（P2-3、V1-J12の検査部分。EAD labelは登録範囲外の65537〜65540・critical、[sdk-v1/02 §6.3](design/sdk-v1/02-zero-touch-join.md)）。AssignmentTicket（A2）の形式は未定で、A2の機器はAllowを受けない（fail closed）。P7-1として事務所tooling（`DeviceCaSigner` seamと開発用file signer、機器鍵の所持証明（PoP）の検証、DevCert発行、注入鍵のRLI1、`rlsec`/`rlident`のNVS image（blob＋`nvs_partition_gen`用CSV）、`routeloomctl provision-devca-keygen／pop-challenge／devcert／identity`）を実装し、発行したDevCert・RLI1が共通vectorとbyte一致することと所持証明の無い鍵に発行しないこと（V1-H09）をhost試験済み。4 storeの`rlsec` NVS adapter（`rlident`/`rlsite`/`rlrevo`/`rlres`）は両firmwareの`rlsec`に配線済み（開いて初期化・boot診断、保守consoleが利用。静的RAM約5.2 KiB増、[sdk-v1/07 §6.2](design/sdk-v1/07-host-api-tooling.md)）。機器側の鍵生成・PoP保守verb（USB consoleのportable engine＋PoPのC++ codec・共通vector）とP7-2のSiteCert発行・正式なinventory出力までhost試験済み。P3-4の参加FSMはP4第2段でMeshNode／firmware Ownerへ配線済み。実機のentropy・保守console・鍵注入、旧版機でrlsec満杯かつSDK namespace未作成時の読み取り専用復旧（#152）は未検証／未実装 |
| SDK v1 鍵導出・高速再参加 | [sdk-v1](design/sdk-v1/README.md)のP1-4／P1-5をportable coreとRustに実装・host試験済み：HKDF導出label／info形式（group鍵、RLRES1、AuthorityEnvelope header、AEAD nonce）を凍結し、独立Python生成器の共通vector（`protocol/sdkv1-golden/derivations/`）にC++／Rustがbyte一致（CIで再生成＋diff）。RLRES1再開handshakeは単独の有界state machine（heap・static無し、開始／応答各4件）で、replay・反射・古いepoch／世代・別現場・壊れた入力・順序違い・格下げ・表の枯渇を理由付きで拒否する攻撃試験とfuzzを実行。RouteLoom独自protocolの先行独立レビュー2本は指摘修正をmerge済みで、P8-2の最終受入記録は未了。Member EDHOC／RLRES1とrouted bootstrapはP4第1〜2段でNode／Ownerへ配線済み。OwnerのRTC sleep経路は第3段でhost試験済み。P8-2の最終受入と実機HILは未実施 |
| SDK v1 EDHOC（P2-1） | 05で固定したlibedhoc v2.3.2（commit `c8857b62d66be3664d1694bbe4eea37c56c05d9e`、MIT）と依存のzcbor 0.8.1、host用AES-CCMとしてTF-PSA-Crypto 1.1.0のAES/CCM部分を`components/routeloom/third_party/`へvendor（blob idを`VENDORED.json`に記録し一致をCI検査、NOTICE・release SBOMに記載）。RouteLoom側backend `routeloom/edhoc.hpp`はsuite 2のみ：P-256 ECDH／ES256はmicro-ecc（受信点は必ず検証、署名は決定的）、SHA-256／HKDFは`kdf.hpp`、AES-CCM-16-64-128はhostでvendorしたbuiltin、ESP-IDFでは`routeloom_espnow`のPSA hook。作業bufferはsessionごとの固定arena（2048B、heap・VLA無し）、鍵は固定key store（10 slot）で、1 session 3880B（64bit host）、ILP32見積約3.5KB。host試験：RFC 9529 §3（method 3／suite 2）のmessage_1〜4・PRK・exporter・key updateをbyte一致、§4の不正messageは§4.1.2（C_Iのbstr符号化、libedhocが同値として受理）を除き全拒否、RouteLoomのmethod 0＋RLCW1 MemberCert（kid参照）の往復と改竄・未知kid・偽証明書・誤鍵の拒否。ESP-IDF componentのC3/S3/C5 buildに加え、P4第2段でMember EDHOC／参加経路をfirmware Ownerへ配線済み（動作はhost simulationとbuildで確認）。kcwtでのcredential値渡し（02の`ID_CRED_x = {13: CWT}`）はlibedhocが未対応のため、P3-1でID_CRED_x＝kid＋証明書をcritical EAD項目（label 65541）で運ぶ形に決め、`edhoc::Session`に任意の`EadHandler`を足してDevCert/SiteCertの参加交換（m1〜m4＝55／362／341／353B）をhost試験した。この交換で作業arenaが1280Bでは不足（最大1440B）したため2048B（session 3880B、ILP32見積約3.5KB）に上げた。C3/S3/C5での時間・stack・heap実測（P2-2、#99）は未実施 |
| SDK v1 Site Authority（P3-3） | `routeloom-host --site-authority DIR`にゼロタッチ参加のSite Authorityを実装・host試験済み（[sdk-v1/07 §2.4](design/sdk-v1/07-host-api-tooling.md)、[host §11](spec/host.md)）。EDHOC Responderは新crate `host/routeloom-edhoc`（pure Rust、suite 2、method 0とRFC 9529用のmethod 3、kid参照、EAD）で、RFC 9529 §3を両roleでbyte一致・§4の不正messageを全拒否し、vendor済みlibedhoc（機器側stack）とのmethod 0参加transcript（RLCW1 DevCert/SiteCert、参加EAD、DAMS Exporter）を**両方向でbyte一致**（`protocol/edhoc-interop`、Rustはcargo test、C++はctest `routeloom_edhoc_interop_replay`、live pipe試験はCI必須）。証明書はEADで値渡し（label 65541はP3-1で凍結、DAMSのExporter contextはP3-4で凍結、機器側と一致）。判定engine：承認済み(node, kid)はKGuardなしで同じMemberCertを再発行、削除済みで現場状態を持つ機器にはSAK署名のRemovalNotice、他は発見済み表（1024件LRU）とKGuardへの`join.request`、KGuard無応答はpending（Q7）、後の決定は次の試行で反映、kid衝突は自動allowしない、同時参加4件・MACごと2秒の上限。台帳はSQLite（0600、hash chain、MemberCert・DAMS・RRS1・GK・発見済み・参加要求・idempotency）で、allowはcommit後にだけ応答し、store故障を成功にしない（commit後の再起動で同一MemberCertを再発行することを試験）。削除はRRS1（SAK署名、rs_epoch+1）と次GKのstagingを同じcommitで行う。API1 `site.status`／`join.policy.*`／`join.requests.list`／`join.decide`／`devices.discovered.list`／`members.list/get`／`membership.revoke`／`operations.get`（op-）と新ACL grant `MEMBERSHIP_READ/DECIDE/ADMIN`、`events` streamへのsite event。`routeloom-client`に`SiteAdmin`と`KGuardMock`を追加し、daemonのAPI1 socket越しに未割当→pending→割当→Allow（機器側の`join_allow_verify`で検証）・deny・ACL・削除を端から端まで試験。P3-3時点で未接続だったUSB参加relay・authority channel・GK配布・RRS1配布・cutoverは後続P4／P5／P6で結線済み。DAMSに束縛したauthority channelでのGK封緘・配布はP5 PR1〜4でhost試験済み。実機試験は未実施。機器側EDHOC arenaは2048Bに引き上げ済み（P3-1、証明書値渡しの交換はarena内に収まる） |
| SDK v1 参加搬送（P3-1／P3-2） | ゼロタッチ参加の機器側・Wire側の搬送をportable coreに実装・host試験済み（[sdk-v1/02 §5.4・§7.4](design/sdk-v1/02-zero-touch-join.md)）：RLD1 body v3（ZeroTouch DISCOVER 24B／OFFER 48B）、BootstrapAuth phase 4〜6（EDHOC message／RLRES1／RelayStatus）を1024B上限の組立object（1 message 960B上限）で運び、RLD1とWire relayで共通のchunk／reply（sub＝phase<<4｜step、順不同・重複・内容違い・3秒・完了済みの再応答を扱う有界slot 1KB）、RelayHeader 24Bのrelay object、joiner／proxyのadmission。engineは機器端`ZtJoinerLink`、`JoinProxy`（同時1件、stateless HMAC cookie、cookie検査前にmemoryを使わない、m1は2秒に1件、20秒／機器無応答5秒、RelayStatus hint）、`JoinRelayGateway`（分割object 2件、受領・再送、host sink）。USBは衝突回避で**HostOps 0x60〜0x63・capability bit 8**（設計の0x40〜0x42／bit 6はnode_status_v1が使用済み）として`UsbBridge::attach_join_relay`とRust `routeloom-protocol::join_relay`に実装。独立Python生成器の共通vector（`protocol/sdkv1-golden/join-transport`、52 valid／103 invalid）、USB session vector（`protocol/usb-golden/join-relay`、C++ bridge再生とRust復号）、`fuzz_sdkv1_join`、device→proxy→gateway→authorityのhost模擬（欠落・順序入替・flood・busy・到達不可・timeout・失効・on_message内send/close/discover再入・重複下りframe/chunkのdedup・gateway sink callback中の再入拒否（Busyまたは無視で状態不変）とcallback後の正常動作）でV1-J02・V1-J10・V1-J11（proxy側）・V1-H08・V1-J14（実長）を実行。portable部品はheap・static無し。#113のgateway callback再入拒否と#116のrelay v2／RelayBook（追い出し・proxy再起動後の重複排除）を回帰試験済み。RLD1／routed Wire、gateway自身の参加、firmware Owner、USB daemonへの配線はP4第2段で実装・build／host試験済み。実機・HIL未実施 |
| SDK v1 参加FSM（P3-4／P4結線） | 機器側Joiner FSMをportable coreに実装・host試験済み（設計P3-4 §3〜§8）：状態表・candidate選択・mailbox・RLS1 commit＋全field指紋照合＋readback＋Reconcile・fail-closed Allow検証・Avoid／pending・再入Busy guard。二現場simulatorの53件（V1-J01／J04〜J07／J13・J12残部・J08行列・再入・#109・quiescence・境界・搬送・store）がASan ctestで通過。Rust実Site Authorityとのlive E2E（`site::joiner_interop`：C++ peer＋2 SiteService＋API1 socket＋KGuard、DAMS／証明書照合・commit順序・電断再起動の4件）をCI必須job `joiner-interop` で実行（C++ peer不在時はskip、専用CI jobではpeerをbuildして実行）。P4第2段でMeshNode／firmware Ownerへ配線済み。P3-5のpending ticket再試行は任意・未採用（full EDHOC retry）。実機・HILは未実施。 |
| SDK v1 P4 session／Owner | PR #133／#141／#144／#149でMember EDHOC／RLRES1、永続membership・RLP2、routed bootstrapとE2E、SecurityCoordinator、USB gateway join、firmware Ownerを配線。boot witness、Owner sleep保存・復元、親binding再確認、全work drain、RTC送信counter先行更新、限定NVS purgeと独立移行markerをhost試験済み。DevRamが公開既定でLegacyFixtureは明示選択のみ（#95）；Ownerも認定前はSecurityProfile::Development。C3/S3/C5のOwner構成を含むESP-IDF matrix 15/15はPR時にbuild／静的RAM gate通過。実機F07・HILは#148／#99、旧版機のrlsec満杯時の読み取り専用復旧は#152。 |
| SDK v1 P5 group鍵・broadcast | PR #126／#137／#131／#143でAuthorityEnvelope、USB 0x64〜0x67、Site AuthorityのGK台帳・24時間更新／pull、機器側durable GK FSMとACK、GroupLink／GroupEnd・Member scope、mesh／USB／Owner配線をhost／firmware build試験済み。#151は実C++ portable security部品⇄Rust Site Authorityのjoin→JoinConfirm→GK配布／更新／pull／削除機器の排除をlive pipe E2Eで確認（実firmwareのRF試験ではない）。PR #145のbroadcast経路広告はbit7 grant付き・既定OFFの選択式。100台host simulationでloop／false routeは0だが、全mesh probeの推定管理airtimeは約2.0M µs/sで出荷有効化にはOwnerのsparse probeとRF検証が必要。 |
| SDK v1 P6 削除・cutover | PR #129／#136／#140／#147でRRS1発行／gossip／執行、署名済みRemovalNotice、RLX1 journal消去と600秒holdoff、GrantRenew／site_epoch cutover、Host通知outbox、OwnerのP4 session／経路／resume失効とSelfRevoked→ZTをhost試験済み。適用済みRRS1のorigin／previous hopは旧GKでもgroup配送を拒否。#151の単一C++ portable security部品＋Rust authorityの15件のlive E2Eは電断再起動を含む。実firmware Owner／MeshNodeの複数機器結合は#150、v1.1の割当世代とGK epochの結合は#146、旧規則で再発行済みのsite DB移行は#154、RF／HILは#99に残る。 |
| 周辺の補修 | #51のRCR2／RLF1復旧とroot署名RTM1更新、#110のgroup sleep drain、#113／#116のjoin relay再入・重複排除、#117のExpectedReply lease、#34の連続boot失敗時のtimed deep sleep、#55／#47のUSB credit・探索／migration窓・受入遅延目標、#60の単調時刻とTX完了起床を各実装・回帰試験で確認。#51の旧firmware移行／実NVS電断、#117のRAM／stack・RF、#60のTX間隔は実機未測定。 |
| ライセンス | Apache-2.0選定済み（LICENSE・NOTICE・SECURITY.md・release pipelineを整備）。依存物の帰属はNOTICEとrelease SBOMに記載。vendorしたmicro-ecc（BSD-2）・libedhoc（MIT）・zcbor（Apache-2.0）・TF-PSA-Crypto AES/CCM（Apache-2.0 OR GPL-2.0-or-later、Apache-2.0で利用）はcomponent内に各LICENSEを同梱 |

PR #155／#156／#158の最終レビュー修正はmerge済み。失効判定のProvider委譲、v1の失効NodeId再発行禁止、EDHOCの末尾余剰拒否、lifecycle gateとRLP2の一本化、ZeroTouch demuxと検証後のhandshake調停、RLRES1 nonce順、join packageの`rs_epoch`取得目標をhost試験で確認した。C ABIはgroup receiverのヘッダー契約を訂正し、型・配置・関数は不変。#132は16 slot/pollの探索とm4搬送受理後installを含め充足した。

受入ID 62件の機械照合は全条件の実証ではない。F04はhost試験なし、K05／K09／N06／F01／R06は未検証half、J15／N08／F06はHIL専用（[08 §3](design/sdk-v1/08-implementation-plan.md)）。

静的受け入れ検査のIssue [#159](https://github.com/MOVEI144/RouteLoom/issues/159)／[#160](https://github.com/MOVEI144/RouteLoom/issues/160)／[#161](https://github.com/MOVEI144/RouteLoom/issues/161) は規格・状態機械・仕様／受入証拠をそれぞれ追跡する。

現在の位置付けは**CORE_FIXED_250実装プロトタイプ**。コンパイル・host test成功を、実機通信・RF資格・production-readyの証拠にはしない。

## CIで継続確認するもの

- Portable core：GCC／Clang、Sanitizer ON/OFF、CTest。P3-4のJoiner／relay、P4のsession・boot／RTC／DevRam、P5のauthority・GK・broadcast route、P6のRRS1・gossip・RLX1・cutover／GrantRenew、P7の保守consoleと記録storeを含む。電源断・再入・異常入力の回帰試験、共通vector、fuzz corpusを実行し、ctest reportをartifact保存。試験数はcommitごとに変わるため固定値をここでは保証しない。
- Wire golden vector：C++ `routeloom_golden_tests`とRust `routeloom-wire` testが同一`protocol/golden`（valid＋invalid）を共有し、generator再生成後の`git diff --exit-code`でbyte一致を確認。USBは`routeloom_usb_tests`と`routeloom-protocol`の`usb_golden`が`protocol/usb-golden`を共有。SDK v1の証明書・記録は`tools/gen_sdkv1_vectors.py`が生成する`protocol/sdkv1-golden`を`routeloom_sdkv1_golden_tests`（検証のみ）とRust `sdkv1_golden`（RFC 6979で再署名し一致を要求）が共有し、再生成後の差分なしを確認。SDK v1の鍵導出は`tools/gen_sdkv1_derivation_vectors.py`が生成する`protocol/sdkv1-golden/derivations`を`routeloom_key_schedule_tests`とRust `routeloom-keysched`が共有する。EDHOCはRFC 9529の公開trace（`protocol/edhoc-rfc9529`、RFCから転記しRouteLoomでは生成しない）を`routeloom_edhoc_tests`がbyte一致で再生する。参加EADは`tools/gen_sdkv1_ead_vectors.py`が生成する`protocol/sdkv1-golden/ead`（とfuzz seed `tests/fuzz/corpus/sdkv1_ead`）を`routeloom_sdkv1_ead_tests`とRust `routeloom-join`（Site Authorityとして再発行し一致を要求）が共有し、再生成後の差分・未追跡fileなしを確認。参加搬送は`tools/gen_sdkv1_join_transport_vectors.py`が生成する`protocol/sdkv1-golden/join-transport`（とfuzz seed `tests/fuzz/corpus/sdkv1_join`）を`routeloom_sdkv1_join_transport_tests`とRust `routeloom-protocol`（relay object）が共有し、USBの参加中継は`protocol/usb-golden/join-relay`をC++ bridgeとRust `usb_golden`が共有する。EDHOCの機器⇄Site Authority相互運用は`protocol/edhoc-interop`（生成器ではなく両実装の実行記録）を`routeloom_edhoc_interop_replay`（C++、kid参照版）とRust `routeloom-edhoc`（両版）がbyte一致で再生する。
- 文書・生成表・契約・negative mutation：Python検査群。`tools/check_review_contracts.py`は設計表の受入ID 62件をテストソースの正確なタグと機械照合し、HIL専用／host試験なしを明示する（タグは試験の全条件達成を意味しない）。`rlsec`のNVS entry予算（`tools/nvs_budget.py`）もここで各firmwareの`partitions.csv`に対して計算検査する。
- Host：固定Rust toolchainでfmt、Clippy、test、release build。host binary（daemon／CLI／TUI）をartifact保存。
- Firmware：固定ESP-IDF v6.0.3／commit 76f5dedd9950a3012fee8fb7d5586df21fc67802でC3/S3/C5をbuildし、sizeとbinary artifactを保存。P4／P5／P6のOwner構成とP7保守構成もbuild・静的RAM gateで確認（#149時点の15構成が合格）。buildは実機動作・RFの証拠ではない。

これらはhost/build evidenceであり、HIL／RF evidenceではない。

## 該当機能の公開前に閉じる項目

- **G-WIRE**：Wire v2 byte layout・型番号・C++／Rust共通golden vector（test cipher）は凍結済み。本番crypto suite適用後のvector更新と残りの管理object・再送round・再起動caseはG-SECと併せて行う。
- **G-SEC**：P3-4〜P7の主経路はhost試験・firmware buildの範囲で結線済み。v1は失効履歴に載ったNodeIdを再発行せず、新NodeIdで再provisionする。残る実firmware Owner／複数機器結合（#150）、v1.1の割当世代とGK epochの結合（#146）、旧規則で再発行済みのsite DBの検出・移行（#154）、End EDHOCの同一peer flight中の正規m1調停（#157）、旧版機のrlsec満杯時の復旧（#152）、P2-2／P8-1の実機HIL（#99、RTC時間上限は#148）、P8-2の最終受入（#100）、鍵保管tier／custodyを追跡する。本番Profileの認定前はSecurityProfile::Development／EXPERIMENTALで表示する。
- **G-ROUTE**：portable実装を基準に、restart／GC／timer、分断再結合、複数origin、10hopをmodel testと実機で認定する。
- **G-CONTROL**：SingleAuthorityの耐電断台帳は実装・host試験済みで、membership承認／失効／remote config向けの操作型integration pointを持つ。NVS実機・HIL、およびHAの選挙、log、snapshot、構成員変更、proofは別途認定する。
- **G-USB**：device側bridge、認証transcript、frame保護、累積creditはportable実装済みで、C++／Rust共有golden vectorでhost相互検証済み。実USB driver・HILでの確認、および本番Profile認証（G-SEC）が残る。
- **G-POWER**：coordinator state machine、SleepTicket、電源image永続化、counter非後退、TIME_UNCERTAIN規則はportable実装・host model試験済み。P4 Owner sleep保存・復元もhost試験済み。実機drain・RTCドリフト／起動遅延（#148）、wake原因、NVS、counter安全性、電池側energyのHILが残る。
- **G-BOARD**：現物revision、電源、アンテナ、Pin、Flash/PSRAMを照合する。
- **G-RF**：C3/S3/C5の有向組合せでLR250、broadcast/unicast、callback欠落、Peer churn、干渉、Sleep復帰を認定する。
- **G-SYSTEM**：USB再接続、キュー不足、PC停止、長期運転、設定更新を試験する。
- **G-LICENSE**：Apache-2.0・NOTICE・SBOMを整備済み。残る判断はコード流用方針の運用確認のみ。

### G-SECの残課題と追跡先

| 残課題 | 現状と追跡先 |
|---|---|
| 実機と認定 | [#99](https://github.com/MOVEI144/RouteLoom/issues/99)：P2-2のEDHOC時間・stack・heap実測とP8-1の2現場／6台復電／失効gossip／電断HIL。[#148](https://github.com/MOVEI144/RouteLoom/issues/148)：F07のRTCドリフト・起動遅延からsleep経過時間上限を確定。[#100](https://github.com/MOVEI144/RouteLoom/issues/100)：先行レビュー2本の指摘修正後のP8-2最終受入。鍵保管tierとcustodyも未決（[08 §4](design/sdk-v1/08-implementation-plan.md)） |
| ソフトウェアの後続 | [#150](https://github.com/MOVEI144/RouteLoom/issues/150)：実firmware Owner／MeshNodeを複数台の模擬radioで結び、gossip・gateway・P4 session／route執行を試験。[#146](https://github.com/MOVEI144/RouteLoom/issues/146)：v1.1のNodeId割当世代とGK epochの結合。v1は失効NodeIdの再発行を禁止し、新NodeIdで再provision。[#154](https://github.com/MOVEI144/RouteLoom/issues/154)：旧規則で再発行済みのsite DBの検出・移行。[#157](https://github.com/MOVEI144/RouteLoom/issues/157)：End EDHOCで同一peerのflight中に届く正規m1の認証後調停。[#152](https://github.com/MOVEI144/RouteLoom/issues/152)：旧版機でrlsec満杯・SDK namespace未作成の場合の読み取り専用復旧 |
| 別契約・故障経路 | [#101](https://github.com/MOVEI144/RouteLoom/issues/101)：任意A2のAssignmentTicket形式（A2機器は現状fail closed）。[#127](https://github.com/MOVEI144/RouteLoom/issues/127)／[#128](https://github.com/MOVEI144/RouteLoom/issues/128)：Site Authority補助記録のstore失敗処理／起動時の失効履歴RAM展開。[#121](https://github.com/MOVEI144/RouteLoom/issues/121)：power image読取失敗後のpending保護。[#134](https://github.com/MOVEI144/RouteLoom/issues/134)：複数namespaceのremote config配送契約 |
| 追跡Issueの状態 | [#94](https://github.com/MOVEI144/RouteLoom/issues/94)／[#95](https://github.com/MOVEI144/RouteLoom/issues/95)／[#96](https://github.com/MOVEI144/RouteLoom/issues/96)／[#97](https://github.com/MOVEI144/RouteLoom/issues/97)はopenのままだが、上記のmerged PRでソフトウェアの該当段を実装済み。[#132](https://github.com/MOVEI144/RouteLoom/issues/132)はPR #141／#158で充足、[#139](https://github.com/MOVEI144/RouteLoom/issues/139)のSelfRevoked経路はPR #147／#151でhost試験済み。open状態だけを未実装の根拠にしない |

## 完成の表示方法

機能×board×profileごとに `documented / implemented / host-tested / build-tested / hardware-tested / qualified` を区別する。C3 build成功をC5のRF合格へ継承しない。`ESP_OK`やCI greenは空中のPHY実測ではない。

公開0.xでも認証・暗号を暗黙に無効化しない。DevRamは公開既定、LegacyFixtureは明示選択のみ。Member EDHOCを含むSDK v1 Ownerも[08 §4](design/sdk-v1/08-implementation-plan.md)の条件を満たすまで`SecurityProfile::Development`／`EXPERIMENTAL`として表示し、`Production`とは表示しない。

[実装案内](implementation/README.md)／[受入試験](spec/acceptance.md)／[実装プロファイル](spec/release-profiles.md)を参照。
